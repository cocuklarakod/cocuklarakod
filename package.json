{
  "name": "cocuklar-icin-projeler-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "validate": "node scripts/validator.js",
    "generate": "python -m pip install -r requirements.txt && python scripts/generator.py"
  },
  "dependencies": {
    "ajv": "^8.12.0"
  }
}
